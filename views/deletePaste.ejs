<!DOCTYPE html>
<html>
  <head>
    <title>Pastes</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  </head>
  <body>
    <div class="card"><% if (name.length > 0) { %>
      <div class="card-header text-white bg-success mb-3">
          <b class="pasteTitle"><%= name %></b>
      </div>
      <div class="card-body bg-light">
          <pre class="card-text"><%= text %></pre>
          <form action="/<%= id %>/<%= name %>" method="POST">
            <button type="submit" class="btn btn-primary">Delete it!</button>
          </form>
          <a href="/<%= id %>" class="btn btn-primary">Go back!</a> 
      </div>
    <% }%></div>
  </body>
</html>